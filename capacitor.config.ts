import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.neonrunner.app',
  appName: 'Neon Runner: Project 2084',
  webDir: 'dist'
};

export default config;
